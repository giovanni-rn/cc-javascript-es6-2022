let texte = "bonjour";

const affichage = (motAAfficher) => {
  console.log(motAAfficher);
};

affichage(texte);
affichage(8);

const addition = (nombre1, nombre2) => {
  const somme = nombre1 + nombre2;
  return somme;
};

console.log(addition(4, 7));
